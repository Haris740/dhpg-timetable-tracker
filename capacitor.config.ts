import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mmchessman.timetablepro',
  appName: 'Timetable Pro',
  webDir: 'dist'
};

export default config;
